# 任务：将 LangChain 项目从平铺结构重构为分层结构

## 当前目录
E:\Python\Qoder\Personal_Knowledge_Base\

## 目标结构
在项目根目录下创建 src/ 目录，按以下分层组织所有源码：

src/
├── core/                    # 单 Agent 核心
│   ├── agent.py             # 原根目录 agent.py
│   ├── error_handler.py     # 原根目录 error_handler.py
│   └── memory_manager.py    # 原根目录 memory_manager.py
│
├── multi_agent/             # 多 Agent 协作
│   ├── supervisor.py        # 原根目录 supervisor.py
│   ├── agents.py            # 原根目录 agents.py
│   ├── agent_adapters.py    # 原根目录 agent_adapters.py
│   ├── message_bus.py       # 原根目录 message_bus.py
│   ├── multi_agent_state.py # 原根目录 multi_agent_state.py
│   └── multi_agent_graph.py # 原根目录 multi_agent_graph.py
│
├── tools/                   # 工具与服务层
│   ├── tools.py             # 原根目录 tools.py
│   ├── retriever.py         # 原根目录 retriever.py
│   ├── reranker.py          # 原根目录 reranker.py
│   ├── kb_manager.py        # 原根目录 kb_manager.py
│   ├── vector_store.py      # 原根目录 vector_store.py
│   └── document_loader.py   # 原根目录 document_loader.py
│
└── ui/                      # 用户界面
    ├── streamlit_app.py     # 原根目录 streamlit_app.py
    ├── cli.py               # 原根目录 cli.py
    ├── utils_web.py         # 原根目录 utils_web.py
    └── pages/               # 原根目录 pages/（整体移入）
        ├── chat.py
        ├── kb.py
        ├── memory.py
        └── history.py

不动文件：data/、documents/、理论学习文档/、.env、.gitignore、pyproject.toml、uv.lock、start.py、start.bat、README.md、code-readme.md

## 执行步骤

### 步骤 1：创建目录结构
在 E:\Python\Qoder\Personal_Knowledge_Base\ 下创建：
- src/core/
- src/multi_agent/
- src/tools/
- src/ui/pages/

每个目录都创建空的 __init__.py 文件。

### 步骤 2：移动文件到对应目录
按上述映射关系移动所有 .py 文件。注意 pages/ 整个目录移入 src/ui/。

### 步骤 3：更新所有 import 路径
全局搜索替换所有被移动文件的内部引用。核心规则：

| 旧导入 | 新导入 |
|--------|--------|
| `from agent import` / `import agent` | `from src.core.agent import` / `from src.core import agent` |
| `from error_handler import` | `from src.core.error_handler import` |
| `from memory_manager import` | `from src.core.memory_manager import` |
| `from supervisor import` | `from src.multi_agent.supervisor import` |
| `from agents import` | `from src.multi_agent.agents import` |
| `from agent_adapters import` | `from src.multi_agent.agent_adapters import` |
| `from message_bus import` | `from src.multi_agent.message_bus import` |
| `from multi_agent_state import` | `from src.multi_agent.multi_agent_state import` |
| `from multi_agent_graph import` | `from src.multi_agent.multi_agent_graph import` |
| `from tools import` | `from src.tools.tools import` |
| `from retriever import` | `from src.tools.retriever import` |
| `from reranker import` | `from src.tools.reranker import` |
| `from kb_manager import` | `from src.tools.kb_manager import` |
| `from vector_store import` | `from src.tools.vector_store import` |
| `from document_loader import` | `from src.tools.document_loader import` |
| `from utils_web import` | `from src.ui.utils_web import` |

同时更新 src/ 内部各文件之间的相互引用，例如 src/core/agent.py 中如果引用了 tools.py，需改为 `from src.tools.tools import`。

### 步骤 4：更新启动脚本
修改 start.py，将 `import streamlit_app` 改为：
```python
import sys
sys.path.insert(0, os.path.dirname(__file__))
from src.ui import streamlit_app
```